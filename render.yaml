services:
  - type: web
    name: greeter-http
    env: python
    plan: free
    rootDir: Sistemas
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
